class Lot {
    constructor(id, title, startingPrice, description, userId) {
      this.id = id;
      this.title = title;
      this.startingPrice = startingPrice;
      this.description = description;
      this.userId = userId;
    }
  }
  
  module.exports = Lot;
  